import in_out as io
import numpy as np

def mask(specs, angles):
    a = angles.reshape(a.size)
    n=0
    for i, e in enumerate(a):
        if e != a[0]:
            n = i
            break
        else:
            n += 1
    cal = 
    
    return s, a
    
def tsys(specs, angles):
    pass
    
